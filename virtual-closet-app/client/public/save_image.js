const fs = require('fs');
const path = require('path');

// Since we can't directly access the image from the attachment,
// we'll document that the image needs to be placed here
console.log("Please save the team photo as group_pic.png in this directory");
console.log("Current directory:", __dirname);
